# Koentjoro Landing Page - monorepo .gitignore
# Generated/curated for a repo with a Vite/React frontend (pnpm) and FastAPI Python backend.

### Node / Frontend
node_modules/
.pnpm-store/
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml

# Vite / build output
dist/
.vite/
build/

# lockfiles (if using a different package manager locally)
package-lock.json
yarn.lock

### Python / Backend
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# Virtual environments
venv/
.venv/
env/
ENV/
venv*/
__pypackages__/

# Pytest cache
.pytest_cache/

# Coverage
coverage/
coverage.xml
htmlcov/
.coverage

# Python env / secrets
.env
.env.*
!.env.example

### Docker
# Local docker-compose overrides
docker-compose.override.yml
docker-compose.*.yml

# Docker build artifacts
**/docker-compose.override.yml

### IDEs and editors
.vscode/
.idea/
*.iml

### OS generated files
.DS_Store
Thumbs.db
desktop.ini

### Logs
logs/
*.log
npm-debug.log*

### Test / CI
.coverage
coverage/
report/

### Misc
# Ignore built assets, local temp files
*.tmp
*.swp
*.swo

# Keep docs/ in repo but ignore generated docs output (if any)
docs/

# Allow repository lockfiles (pnpm) but ignore private package mangled files
# (pnpm lockfile is committed in this repo—keep it tracked)

# End of .gitignore